import { ChatList } from '@/components/ChatList';
import { MainContainer } from '@/components/MainContainer';

const page = () => {
  return (
    <MainContainer>
      <ChatList />
    </MainContainer>
  );
};
export default page;
